<Window
    x:Class="TalkTimer.Wpf.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:TalkTimer.UI"
    SizeToContent="WidthAndHeight"
    Title="TalkTimer" >

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock x:Name="TimerText" Text="00:00" FontSize="32" FontWeight="Bold" Grid.Row="0" HorizontalAlignment="Center" />

        <Image x:Name="MicrophoneIcon" Grid.Row="1" Width="50" Height="50" Source="Assets/muted_microphone.png" HorizontalAlignment="Center" Margin="0,10"/>

        <Slider x:Name="VolumeSlider" Grid.Row="2" Margin="0,10" Minimum="0" Maximum="100" Value="50" />

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10">
            <TextBlock Text="Device:" VerticalAlignment="Center" />
            <ComboBox x:Name="InputDeviceComboBox" Width="200" Margin="5,0"/>
        </StackPanel>
    </Grid>
</Window>